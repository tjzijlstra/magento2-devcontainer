# GitHub CLI

The [GitHub CLI](https://cli.github.com/) (`gh`) is installed automatically via the [`github-cli` devcontainer feature](https://github.com/devcontainers/features/tree/main/src/github-cli). It provides command-line access to GitHub operations such as pull requests, issues, and repository management.

## Usage

```bash
gh pr create
gh issue list
gh repo view
```